apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "live-automation-dashboard.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "live-automation-dashboard.name" . }}
    helm.sh/chart: {{ include "live-automation-dashboard.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "live-automation-dashboard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
